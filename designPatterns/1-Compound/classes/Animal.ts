//* Las clases abstractas no pueden instanciarse y deben contener al menos un método abstracto, para que las clases que hereden de esta puedan implementarlo.
export abstract class Animal {
  abstract display(): void;
}
